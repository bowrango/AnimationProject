function element = rotate(element, theta)
    % Rotate an element some angle theta
    
    % It would make sense to have the element rotate about its own local
    % axis, not the origin. We can discuss this later. 
    
end

