function erase(element)
    % Remove an element from the scene. This will be used within each
    % transformation function to clear the element before the updated
    % element is drawn.
    
    % This won't modified the element vector in any way, just remove it
    % from the scene. 
end

